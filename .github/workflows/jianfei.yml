name: Build Android APK
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-24.04
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Verify required files exist
      run: |
        echo "检查必要文件..."
        required_files=("buildozer.spec" "fonts/simkai.ttf" "data/icon.png" "main.py")
        for file in "${required_files[@]}"; do
          if [ -f "$file" ]; then
            echo "✅ $file 存在"
          else
            echo "❌ $file 不存在"
            exit 1
          fi
        done
        echo "所有必要文件检查通过！"
        
    - name: Build APK using Java 11 Docker
      run: |
        echo "使用 Java 11 Docker 环境构建 APK..."
        
        # 使用 Ubuntu 20.04 并安装 Java 11
        docker run --rm \
          --volume "$(pwd)":/home/user/hostcwd \
          --workdir /home/user/hostcwd \
          --env DEBIAN_FRONTEND=noninteractive \
          --env BUILDOZER_ALLOW_ROOT=1 \
          ubuntu:20.04 /bin/bash -c "
            # 更新并安装必要工具，使用 Java 11
            apt-get update && apt-get install -y \
              python3 \
              python3-pip \
              build-essential \
              git \
              zip \
              unzip \
              openjdk-11-jdk \  # 使用 Java 11 而不是 Java 8
              zlib1g-dev \
              autoconf \
              automake \
              libtool \
              pkg-config \
              cmake \
              libffi-dev \
              libssl-dev \
              wget \
              curl \
              && rm -rf /var/lib/apt/lists/*

            # 设置 JAVA_HOME 环境变量
            export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64
            export PATH=\$JAVA_HOME/bin:\$PATH

            # 安装 Buildozer 和依赖
            pip3 install --upgrade pip
            pip3 install buildozer cython==0.29.33

            # 接受 Android SDK 许可证
            mkdir -p /root/.android
            mkdir -p /root/.android/licenses
            echo '8933bad161af4178b1185d1a37fbf41ea5269c55' > /root/.android/licenses/android-sdk-license
            echo '84831b9409646a918e30573bab4c9c91346d8abd' > /root/.android/licenses/android-sdk-preview-license

            # 开始构建 - 使用 yes 命令自动回答所有提示
            yes | buildozer -v android debug
          "
      continue-on-error: true
      
    - name: Check if APK exists
      id: check_apk
      run: |
        if find bin/ -name "*.apk" | grep -q .; then
          echo "apk_exists=true" >> $GITHUB_OUTPUT
          APK_FILE=$(find bin/ -name "*.apk" | head -1)
          echo "APK文件找到: $APK_FILE"
          echo "APK文件大小: $(du -h "$APK_FILE" | cut -f1)"
        else
          echo "apk_exists=false" >> $GITHUB_OUTPUT
          echo "未找到APK文件"
          echo "当前目录内容:"
          ls -la
          echo "bin目录内容:"
          ls -la bin/ 2>/dev/null || echo "bin目录不存在"
        fi
        
    - name: Upload APK artifact
      if: steps.check_apk.outputs.apk_exists == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: jianfei-app-apk
        path: bin/*.apk
        retention-days: 30
        
    - name: Upload build logs
      uses: actions/upload-artifact@v4
      with:
        name: build-logs
        path: |
          .buildozer/
          bin/
        retention-days: 30
        
    - name: Final status report
      run: |
        if [ "${{ steps.check_apk.outputs.apk_exists }}" = "true" ]; then
          echo "🎉 APK构建成功！"
          echo "📱 可在Artifacts中下载APK文件"
        else
          echo "💥 APK构建失败"
          echo "🔍 请查看构建日志了解详细错误信息"
        fi
