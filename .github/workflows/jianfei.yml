name: Build Android APK
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-24.04
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python and dependencies
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          python3-pip \
          build-essential \
          git \
          zip \
          unzip \
          openjdk-11-jdk \
          zlib1g-dev \
          autoconf \
          automake \
          libtool \
          libtool-bin \
          pkg-config \
          cmake \
          libffi-dev \
          libssl-dev \
          wget \
          curl
        
    - name: Install Buildozer and requirements
      run: |
        pip install buildozer==1.5.0
        pip install cython==0.29.33
        pip install kivy==2.1.0 kivymd==1.1.1 peewee==3.14.4
        
    - name: Verify required files exist
      run: |
        echo "检查必要文件..."
        required_files=("buildozer.spec" "fonts/simkai.ttf" "data/icon.png" "main.py")
        for file in "${required_files[@]}"; do
          if [ -f "$file" ]; then
            echo "✅ $file 存在"
          else
            echo "❌ $file 不存在"
            exit 1
          fi
        done
        echo "所有必要文件检查通过！"
        
    - name: Setup Android NDK 25
      run: |
        echo "设置 Android NDK 25..."
        
        # 删除旧的 NDK 23
        rm -rf ~/.buildozer/android/platform/android-ndk-r23b
        
        # 下载并安装 NDK 25b
        NDK_URL="https://dl.google.com/android/repository/android-ndk-r25b-linux.zip"
        NDK_DIR="$HOME/.buildozer/android/platform"
        
        mkdir -p $NDK_DIR
        cd $NDK_DIR
        
        # 下载 NDK 25b
        wget -q $NDK_URL -O android-ndk-r25b.zip
        unzip -q android-ndk-r25b.zip
        rm android-ndk-r25b.zip
        
        echo "Android NDK 25b 安装完成"
        
    - name: Build APK with Buildozer
      run: |
        echo "开始构建 APK..."
        
        # 设置环境变量
        export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64
        export ANDROID_NDK_HOME="$HOME/.buildozer/android/platform/android-ndk-r25b"
        export PATH=$JAVA_HOME/bin:$ANDROID_NDK_HOME:$PATH
        
        # 清理之前的构建
        buildozer android clean || true
        
        echo "开始正式构建..."
        # 使用超时机制
        timeout 3600 buildozer -v android debug
      continue-on-error: true
      
    - name: Check if APK exists
      id: check_apk
      run: |
        if find bin/ -name "*.apk" 2>/dev/null | grep -q .; then
          echo "apk_exists=true" >> $GITHUB_OUTPUT
          APK_FILE=$(find bin/ -name "*.apk" | head -1)
          echo "APK文件找到: $APK_FILE"
          echo "APK文件大小: $(du -h "$APK_FILE" | cut -f1)"
          echo "🎉 APK构建成功！"
        else
          echo "apk_exists=false" >> $GITHUB_OUTPUT
          echo "未找到APK文件"
          echo "检查构建状态..."
          if [ -d ".buildozer" ]; then
            echo "构建目录存在，检查日志..."
            find .buildozer -name "*.log" -exec echo "=== {} ===" \; -exec tail -30 {} \; 2>/dev/null | head -150 || echo "无日志文件"
          else
            echo "构建目录不存在"
          fi
        fi
        
    - name: Upload APK artifact
      if: steps.check_apk.outputs.apk_exists == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: jianfei-app-apk
        path: bin/*.apk
        retention-days: 30
        
    - name: Upload build logs
      if: steps.check_apk.outputs.apk_exists == 'false'
      uses: actions/upload-artifact@v4
      with:
        name: build-logs
        path: |
          .buildozer/
        retention-days: 30
        
    - name: Final status report
      run: |
        if [ "${{ steps.check_apk.outputs.apk_exists }}" = "true" ]; then
          echo "🎉 恭喜！APK构建成功！"
          echo "📱 可在Artifacts中下载APK文件"
          echo "✅ 所有问题已解决！"
        else
          echo "💥 APK构建失败"
          echo "🔍 请查看构建日志了解详细错误信息"
          echo "💡 建议：尝试在本地环境中使用相同配置构建"
        fi
