name: Build Android APK
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          python3-pip \
          build-essential \
          git \
          zip \
          unzip \
          openjdk-11-jdk \
          autoconf \
          libtool \
          pkg-config \
          zlib1g-dev \
          libncurses5-dev \
          libncursesw5-dev \
          libtinfo5 \
          cmake \
          libffi-dev \
          libssl-dev \
          libsqlite3-dev
          
    - name: Install Buildozer and dependencies
      run: |
        pip install --user buildozer
        pip install --user cython
        echo "$(python -m site --user-base)/bin" >> $GITHUB_PATH
        
    - name: Verify required files exist
      run: |
        echo "检查必要文件..."
        if [ -f "buildozer.spec" ]; then
          echo "✅ buildozer.spec 文件存在"
        else
          echo "❌ buildozer.spec 文件不存在"
          exit 1
        fi
        
        if [ -f "fonts/simkai.ttf" ]; then
          echo "✅ 字体文件存在: fonts/simkai.ttf"
        else
          echo "❌ 字体文件不存在: fonts/simkai.ttf"
          exit 1
        fi
        
        if [ -f "data/icon.png" ]; then
          echo "✅ 图标文件存在: data/icon.png"
        else
          echo "❌ 图标文件不存在: data/icon.png"
          exit 1
        fi
        
        if [ -f "main.py" ]; then
          echo "✅ 主程序文件存在: main.py"
        else
          echo "❌ 主程序文件不存在: main.py"
          exit 1
        fi
        
        echo "所有必要文件检查通过！"
        
    - name: Build APK with Buildozer
      run: |
        buildozer -v android debug
      continue-on-error: true
      
    - name: Check if APK exists
      id: check_apk
      run: |
        if ls bin/*.apk 1> /dev/null 2>&1; then
          echo "apk_exists=true" >> $GITHUB_OUTPUT
          APK_FILE=$(ls bin/*.apk)
          echo "APK文件找到: $APK_FILE"
          echo "APK文件大小: $(du -h $APK_FILE | cut -f1)"
        else
          echo "apk_exists=false" >> $GITHUB_OUTPUT
          echo "未找到APK文件"
          # 检查构建目录内容
          echo "构建目录内容:"
          ls -la .buildozer/ || echo ".buildozer目录不存在"
          ls -la bin/ || echo "bin目录不存在"
        fi
        
    - name: Upload APK artifact (强制上传)
      if: steps.check_apk.outputs.apk_exists == 'true'
      uses: actions/upload-artifact@v3
      with:
        name: jianfei-app-apk
        path: bin/*.apk
        retention-days: 30
        
    - name: Create build info file
      if: steps.check_apk.outputs.apk_exists == 'false'
      run: |
        mkdir -p bin
        echo "构建信息" > bin/build_info.txt
        echo "==========" >> bin/build_info.txt
        echo "构建时间: $(date)" >> bin/build_info.txt
        echo "构建状态: 失败 - 未生成APK文件" >> bin/build_info.txt
        echo "请检查GitHub Actions日志了解详细错误信息" >> bin/build_info.txt
        
    - name: Upload build logs and info
      uses: actions/upload-artifact@v3
      with:
        name: build-logs
        path: |
          .buildozer/
          bin/
        retention-days: 30
        
    - name: Final status report
      run: |
        if [ "${{ steps.check_apk.outputs.apk_exists }}" = "true" ]; then
          echo "🎉 APK构建成功！"
          echo "📱 可在Artifacts中下载APK文件"
          echo "📊 构建完成时间: $(date)"
        else
          echo "💥 APK构建失败"
          echo "🔍 请查看构建日志了解详细错误信息"
          echo "⏰ 构建失败时间: $(date)"
        fi
