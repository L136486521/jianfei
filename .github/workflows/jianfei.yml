name: Build Android APK
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-24.04
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install absolute minimum dependencies
      run: |
        sudo apt-get update
        # 只安装最基础的包
        sudo apt-get install -y \
          python3-pip \
          build-essential \
          git \
          zip \
          unzip \
          openjdk-11-jdk \
          zlib1g-dev
        
    - name: Install Buildozer
      run: |
        pip install buildozer
        echo "$(python -m site --user-base)/bin" >> $GITHUB_PATH
        
    - name: Fix buildozer.spec configuration
      run: |
        echo "修复 buildozer.spec 配置..."
        # 备份原文件
        cp buildozer.spec buildozer.spec.backup
        
        # 移除冲突的版本配置 - 只保留简单的版本号
        # 删除 version.regex 和 version.filename 行
        sed -i '/version.regex/d' buildozer.spec
        sed -i '/version.filename/d' buildozer.spec
        
        echo "修复后的 buildozer.spec 相关内容:"
        grep -A2 -B2 "version" buildozer.spec || echo "没有找到version相关配置"
        
    - name: Verify required files exist
      run: |
        echo "检查必要文件..."
        required_files=("buildozer.spec" "fonts/simkai.ttf" "data/icon.png" "main.py")
        for file in "${required_files[@]}"; do
          if [ -f "$file" ]; then
            echo "✅ $file 存在"
          else
            echo "❌ $file 不存在"
            exit 1
          fi
        done
        echo "所有必要文件检查通过！"
        
    - name: Build APK with Buildozer
      run: |
        # 先检查配置
        buildozer check
        echo "配置检查完成，开始构建..."
        
        # 构建APK
        buildozer -v android debug
      continue-on-error: true
      
    - name: Check if APK exists
      id: check_apk
      run: |
        if find bin/ -name "*.apk" | grep -q .; then
          echo "apk_exists=true" >> $GITHUB_OUTPUT
          APK_FILE=$(find bin/ -name "*.apk" | head -1)
          echo "APK文件找到: $APK_FILE"
          echo "APK文件大小: $(du -h "$APK_FILE" | cut -f1)"
        else
          echo "apk_exists=false" >> $GITHUB_OUTPUT
          echo "未找到APK文件"
          echo "检查构建日志..."
          find .buildozer -name "*.log" -exec echo "=== {} ===" \; -exec head -50 {} \; 2>/dev/null || echo "没有找到日志文件"
        fi
        
    - name: Upload APK artifact
      if: steps.check_apk.outputs.apk_exists == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: jianfei-app-apk
        path: bin/*.apk
        retention-days: 30
        
    - name: Upload build logs
      uses: actions/upload-artifact@v4
      with:
        name: build-logs
        path: |
          .buildozer/
          bin/
        retention-days: 30
        
    - name: Final status report
      run: |
        if [ "${{ steps.check_apk.outputs.apk_exists }}" = "true" ]; then
          echo "🎉 APK构建成功！"
          echo "📱 可在Artifacts中下载APK文件"
        else
          echo "💥 APK构建失败"
          echo "🔍 请查看构建日志了解详细错误信息"
        fi
